<Window x:Class="RegistryRepairTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Registry Repair Tool" 
        Height="570" Width="1100"
        MaxHeight="570" MaxWidth="1100"
        MinHeight="570" MinWidth="1100"
        WindowStartupLocation="CenterScreen"
        Background="#FFF8F9FA"
        FontFamily="Segoe UI">

    <Window.Resources>
        <!-- Цветовая палитра -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#FF3F51B5"/>
        <SolidColorBrush x:Key="PrimaryLightBrush" Color="#FFC5CAE9"/>
        <SolidColorBrush x:Key="PrimaryDarkBrush" Color="#FF303F9F"/>

        <!-- Стиль для кнопок навигации -->
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="8,4"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryDarkBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                            Background="{TemplateBinding Background}"
                            CornerRadius="4">
                            <ContentPresenter x:Name="contentPresenter"
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                        Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
                                <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для активной кнопки -->
        <Style x:Key="ActiveNavButtonStyle" TargetType="Button" BasedOn="{StaticResource NavButtonStyle}">
            <Setter Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <!-- Стиль для заголовка -->
        <Style x:Key="AppTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryDarkBrush}"/>
            <Setter Property="Margin" Value="16,24,16,32"/>
        </Style>

        <!-- Градиент для анимации -->
        <LinearGradientBrush x:Key="AnimatedGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FF3F51B5" Offset="0"/>
            <GradientStop Color="#FF303F9F" Offset="0.5"/>
            <GradientStop Color="#FF7986CB" Offset="1"/>
        </LinearGradientBrush>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Боковая панель -->
        <Border Grid.Column="0" 
                Background="White"
                BorderThickness="0,0,1,0"
                BorderBrush="#FFEEEEEE">
            <StackPanel>
                <!-- Заголовок приложения с анимацией -->
                <Border CornerRadius="4" Margin="16,24,16,32" Padding="16,8" 
        Background="{StaticResource AnimatedGradient}"
        x:Name="AnimatedHeader">
                    <TextBlock Text="REGISTRY TOOL" 
              FontSize="20"
              FontWeight="Medium"
              Foreground="White"
              HorizontalAlignment="Center"
              x:Name="AnimatedText"/>
                </Border>

                <!-- Кнопки навигации -->
                <Button Command="{Binding NavigateCommand}" CommandParameter="Home">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource NavButtonStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CurrentPageKey}" Value="Home">
                                    <Setter Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="" 
                  FontFamily="Segoe MDL2 Assets"
                  FontSize="16"
                  Margin="0,0,8,0"/>
                        <TextBlock Text="Главная"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding NavigateCommand}"
        CommandParameter="AutoFix">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource NavButtonStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CurrentPageKey}" Value="AutoFix">
                                    <Setter Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="" 
                  FontFamily="Segoe MDL2 Assets"
                  FontSize="16"
                  Margin="0,0,8,0"/>
                        <TextBlock Text="Автоисправление"/>
                    </StackPanel>
                </Button>

                <Button Command="{Binding NavigateCommand}"
        CommandParameter="ManualEdit">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource NavButtonStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CurrentPageKey}" Value="ManualEdit">
                                    <Setter Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="" 
                  FontFamily="Segoe MDL2 Assets"
                  FontSize="16"
                  Margin="0,0,8,0"/>
                        <TextBlock Text="Ручное редактирование"/>
                    </StackPanel>
                </Button>



                <!-- Разделитель -->

                <Border Height="1" Background="#EEE" Margin="16,12"/>

                <Button Command="{Binding NavigateCommand}"
CommandParameter="Settings">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource NavButtonStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CurrentPageKey}" Value="Settings">
                                    <Setter Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="" 
          FontFamily="Segoe MDL2 Assets"
          FontSize="16"
          Margin="0,0,8,0"/>
                        <TextBlock Text="Настройки"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <!-- Основная область -->
        <Grid Grid.Column="1" Margin="8">
            <Border Background="White" CornerRadius="4">
                <Frame Content="{Binding CurrentPage}"
                       NavigationUIVisibility="Hidden"/>
            </Border>
        </Grid>
    </Grid>
</Window>